public List<UserResponse> getAllUsers(RequestContext context) {
        logger.info("Received RequestContext for getAllUsers: {}", context);

        // Validate context-entity-ID
        String entityId = authenticationUtils.getContextEntityId(context);
        if (entityId == null || entityId.isEmpty()) {
            logger.error("Context_id_entity required but missing in RequestContext");
            throw new SecurityException("Context_id_entity required");
        }
        logger.info("Extracted context-entity-ID: {}", entityId);

        // Fetch all users
        List<UserEntity> users = usersRepository.findAll();
        return users.stream()
            .map(this::mapUserToDto)
            .collect(Collectors.toList());
    }
