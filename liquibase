{isIncidentsLoading ? (
  <div className="flex justify-center items-center h-64">
    <div className="text-gray-500 text-lg animate-pulse">Chargement des incidents...</div>
  </div>
) : (
  incidents.map((incident) => (
    // ton affichage des incidents ici
  ))
)}
